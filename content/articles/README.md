# Articles (content projects)

One directory per article, each with an explicit name: each article holds its own tapes/samples (if any), build output, and optional platform subdirs (mastodon, linkedin, twitter).

- **`one_to_rule/`** – Hero video + linked-post assets; includes `tapes/`, `scripts/`, samples, and `output/` with `final/` (deliverables) + `work/` (intermediates). Hero build: **`one_to_rule/scripts/generate_one_to_rule_video.sh`** (tracked). See [one_to_rule/README.md](one_to_rule/README.md).
